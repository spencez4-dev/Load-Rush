LOAD RUSH V3.3 — REMINDERS UI FIX

FIXED
- Reminders button reliably opens.
- Upcoming reminders are visible in a dedicated list.
- Add New tab opens the reminder creation form.
- Unlimited reminders can be created.
- Add Reminder button resets every time the dialog opens.
- After creating a reminder, the app returns to the Upcoming list so you can verify it.
- Reminder event bindings are now defensive and won't break if another UI element fails.

UPLOAD
Replace the existing files in the same GitHub repository.

Open once with:
?v=reminders-ui-v3

Badge:
LOAD RUSH · V3.3
